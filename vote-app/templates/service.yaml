apiVersion: v1
kind: Service
metadata:
  name: {{ template "vote-app.fullname" . }}
  labels:
    app: {{ template "vote-app.name" . }}
    chart: {{ template "vote-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "vote-app.name" . }}
    release: {{ .Release.Name }}
